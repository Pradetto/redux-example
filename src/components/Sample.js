import React from "react";
import { ChakraProvider } from "@chakra-ui/react";

import { Button } from "@chakra-ui/react";

const Sample = () => {
  return (
    <div>
      <Button fontSize="5rem">Hello</Button>
    </div>
  );
};

export default Sample;
